name: Build Obfuscate BPB Panel
on:
  push:
    branches:
      - main
  schedule:
    # Runs everyday at 1:00 AM
    - cron: "0 1 * * *"
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "latest"
      - name: Install dependencies
        run: |
          npm install -g javascript-obfuscator
      - name: Clone BPB work.js
        run: |
          # Try multiple possible file paths
          if wget -O origin.js https://raw.githubusercontent.com/bia-pain-bache/BPB-Worker-Panel/main/src/worker.js; then
            echo "Downloaded from src/worker.js"
          elif wget -O origin.js https://raw.githubusercontent.com/bia-pain-bache/BPB-Worker-Panel/main/worker.js; then
            echo "Downloaded from worker.js"
          elif wget -O origin.js https://raw.githubusercontent.com/bia-pain-bache/BPB-Worker-Panel/main/build/worker.js; then
            echo "Downloaded from build/worker.js"
          else
            echo "Failed to download worker file from any known location"
            exit 1
          fi
      - name: Verify file was downloaded
        run: |
          if [ ! -f origin.js ]; then
            echo "origin.js file not found"
            exit 1
          fi
          echo "File size: $(wc -c < origin.js) bytes"
          echo "First 5 lines of the file:"
          head -5 origin.js
      - name: Obfuscate BPB worker js
        run: |
          javascript-obfuscator origin.js --output _worker.js \
            --compact true \
            --control-flow-flattening true \
            --control-flow-flattening-threshold 1 \
            --dead-code-injection true \
            --dead-code-injection-threshold 1 \
            --identifier-names-generator hexadecimal \
            --rename-globals true \
            --string-array true \
            --string-array-encoding 'rc4' \
            --string-array-threshold 1 \
            --transform-object-keys true \
            --unicode-escape-sequence true
      - name: Verify obfuscated file was created
        run: |
          if [ ! -f _worker.js ]; then
            echo "Obfuscated file was not created"
            exit 1
          fi
          echo "Obfuscated file size: $(wc -c < _worker.js) bytes"
      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          branch: main
          commit_message: ':arrow_up: update latest bpb panel'
          commit_author: 'github-actions[bot] <github-actions[bot]@users.noreply.github.com>'
          push_options: '--set-upstream'
